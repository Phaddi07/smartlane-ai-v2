[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "smartlane-ai"
version = "2.0.0"
description = "SmartLane AI driving analysis system"
requires-python = ">=3.11"

[tool.setuptools.packages.find]
where = ["src"]